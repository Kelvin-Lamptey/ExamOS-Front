[package]
name = "exam-os"
version = "0.1.0"
description = "SmartScript Exam OS student application"
authors = ["KenolTech"]
edition = "2021"

[lib]
name = "exam_os_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
